const createTask = (title, description, dueDate, priority) => {
  let uuid = 1;
  let title = title;
  let description = description;
  let dueDate = dueDate;
  let priority = priority;
  let completed = false;
  let items = [];

  const editTask = () => {};
};
